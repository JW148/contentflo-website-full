import React from "react";

import { House } from "lucide-react";

const Icon: React.FC = () => {
  return (
    <div className="flex w-full h-full">
      <House size={20} />
    </div>
  );
};

export default Icon;
